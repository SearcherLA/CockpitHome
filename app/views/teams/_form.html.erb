<%= form_with(model: team, local: true) do |form| %>
  <% if team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br/>
  <div class="col-xs-12 col-lg-12">
    <div class="form-group has-feedback">
      <%= form.label :teamname, "Teamname" %><br />
      <%= form.text_field :teamname, id: :team_teamname, class: "form-control", autofocus: true %>
      <span class="fa fa-users form-control-feedback"></span>
    </div>

    <div class="form-group has-feedback">
      <%= form.label :shortcut, "Abkürzung" %><br />
      <%= form.text_field :shortcut, id: :team_shortcut, class: "form-control" %>
      <span class="fa fa-scissors form-control-feedback"></span>
    </div>

    <div class="actions">
      <%= link_to 'Abbrechen', :back, :class => 'btn btn-danger btn-flat btn-sm pull-left' %>
      <%= form.submit 'Speichern', :class => "btn btn-success btn-flat btn-sm pull-right" %>
    </div>
    <br/>
  </div>
<% end %>
