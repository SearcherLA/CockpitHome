<!-- Content Header (Page header) -->
<section class="content-header">
  <h1>
    Profil bearbeiten <small></small>
  </h1>
  <ol class="breadcrumb">
    <li>
      <%= link_to static_dashboard_path do %>
        <i class="fa fa-dashboard"></i> Home
      <% end %>
    </li>
    <li class="active">Profil bearbeiten</li>
  </ol>
</section>
<section class="content">
  <div class="row">
    <div class="col-lg-6 col-xs-12 col-lg-offset-3">
      <div class="box box-widget widget-user">
        <div class="widget-user-header bg-light-blue-gradient">
          <h3><%= current_user.firstname %> <%= current_user.lastname %></h3>
          <h5>Administrator</h5>
        </div>
        <div class="widget-user-image">
          <%= image_tag(current_user.avatar.url(:medium), :class => "img-circle") %>
        </div>
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { :multipart => true }) do |f| %>
        <%= devise_error_messages! %>
        <div class="box-body">
          <div class="form-group has-feedback">
            <%= f.label :firstname, "Vorname" %>
            <%= f.text_field :firstname, :class => "form-control" %>
            <span class="fa fa-user form-control-feedback"></span>
          </div>
          <div class="form-group has-feedback">
            <%= f.label :lastname, "Nachname" %>
            <%= f.text_field :lastname, :class => "form-control" %>
            <span class="fa fa-user form-control-feedback"></span>
          </div>
          <div class="form-group has-feedback">
            <%= f.label :email, "Email" %>
            <%= f.email_field :email, class: "form-control", autofocus: true, autocomplete: "email" %>
            <span class="fa fa-envelope form-control-feedback"></span>
          </div>
          <br />
          <div class="form-group">
            <%= f.label :avatar, "Avatar ändern" %>
            <%= f.file_field(:avatar) %>
          </div>
          <br />
          <div class="form-group has-feedback">
            <%= f.label :password, "Passwort" %> <i>(nur wenn Passwort geändert werden soll)</i><br />
            <%= f.password_field :password, autocomplete: "off", :class => "form-control" %>
            <% if @minimum_password_length %>
              <em><%= @minimum_password_length %> Zeichen minimum</em>
            <% end %>
            <span class="fa fa-lock form-control-feedback"></span>
          </div>
          <div class="form-group has-feedback">
            <%= f.label :password_confirmation, "Passwort-Wiederholung" %><br />
            <%= f.password_field :password_confirmation, autocomplete: "off", :class => "form-control" %>
            <span class="fa fa-lock form-control-feedback"></span>
          </div>
        </div>
        <div class="box-footer with-border">
          <div class="input-group">
            <%= f.password_field :current_password, autocomplete: "off", :class => "form-control", :placeholder => "Bitte aktuelles Passwort eingeben" %>
            <span class="input-group-btn"><%= f.submit "Änderungen speichern", :class => "btn btn-flat btn-danger"  %></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<% end %>
